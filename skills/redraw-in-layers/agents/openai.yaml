interface:
  display_name: "Layered Redraw"
  short_description: "Editable layered scenes, art, archives, and specs"
  default_prompt: "Use $redraw-in-layers to create editable semantic layers, including verified clean-plate backgrounds for movable photo objects and faithful photo-and-paper stamp archives, while keeping real measurements separate from visual transforms."
