interface:
  display_name: "Layered Redraw"
  short_description: "RGB/depth planning for editable semantic layers"
  default_prompt: "Use $redraw-in-layers to register my RGB or RGB-D references, preserve raw depth evidence, use my prompt to resolve an editable 8–12-layer semantic plan, compare A/B/C design proofs when direction is unsettled, and keep artwork text disabled."
