interface:
  display_name: "Layered Redraw"
  short_description: "Editable art layers with measured object specs"
  default_prompt: "Use $redraw-in-layers to create editable semantic layers and keep any real object dimensions, apparel sizes, placement, angles, and sheet scale as validated specifications separate from visual transforms."
